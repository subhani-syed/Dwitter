asgiref==3.5.2
#backports.zoneinfo==0.2.1 # this version of backports doesn't get supported when python version is >= 3.9 therefore use the below
backports.zoneinfo;python_version<"3.9"
Django==4.0.5
sqlparse==0.4.2
